# CookScale (料理分量らくらく計算ツール) 開発記録 (RECORD.md)

## プロジェクト概要
- **目的**: 料理中に迷わず使えるよう、レシピの分量を指定した人数分に一発で再計算し、計量しやすい形式（大さじ・小さじ等）に自動整形するクライアントサイドSPAツール。
- **リポジトリ**: `C:\Users\tk030\Desktop\料理分量らくらく計算ツール`
- **技術スタック**: Vite + React + TypeScript + Tailwind CSS + shadcn/ui
- **防衛設計**: 完全クライアントサイド動作（サーバーサイドなし・通信不要）のSPAアーキテクチャ。

---

## 開発履歴

<!-- ──────────────────────────────────────────────────
  【エントリ記述ルール】
  - 新しいエントリは最下部に追記する（新しい順ではなく古い順）
  - タイトル: ### YYYY-MM-DD: [作業タイトル（フェーズ・ステップ番号を含む）]
  - 変更概要: 箇条書きで「何をしたか」「なぜしたか」「どんな結果になったか」を記述
  - 関連コミット: コミットメッセージとハッシュ（任意）を記録
  - 進捗ステータス: 完了ステップおよび全体進捗率を記録
────────────────────────────────────────────────── -->

### 2026-08-29: プロジェクト初期化およびUI環境セットアップ
- **変更概要**:
  - Vite (React+TS) にてプロジェクトの雛形を作成。
  - プロジェクト固有のルールファイル群（.agents/AGENTS.md等）の同期スクリプトを実行し初期配置。
  - GitHubにプライベートリポジトリを作成して初期コミットをプッシュ。
  - 開発計画書(`implementation_plan.md`)およびタスクリスト(`task.md`)を作成し、実装方針を確定。
  - Tailwind CSS (v3) および shadcn/ui (Button, Card, Input, Textarea等) の初期セットアップを実施。
- **関連コミット**:
  - `chore: first commit` 等
- **進捗ステータス**: フェーズ1 初期セットアップ完了。

### 2026-08-29: コアロジック実装・テスト、およびUI結合
- **変更概要**:
  - 3つの単一責任に分離したロジックモジュールを作成。
    1. `parser.ts`: テキスト解析（非数値表現の除外・数量と単位の抽出）。
    2. `calculator.ts`: 人数倍率による分量計算。
    3. `formatter.ts`: 小数処理と計量スプーン単位（大さじ・小さじ）の自動整形。
  - `Vitest` を導入し、ロジック群の単体テスト（10項目）を実装・全通過を確認。
  - `App.tsx` にモバイルファーストのUI（入力フォーム、リアルタイム計算、結果リスト）を実装して結合。
  - 全体の動作確認後、Gitへのコミット・プッシュを完了。
- **関連コミット**:
  - `feat: implement CookScale core logic and UI`
- **進捗ステータス**: フェーズ2,3 ロジック・UI実装完了。

### 2026-08-29: ツール起動バッチファイルの作成
- **変更概要**:
  - コマンドラインなしで開発サーバーとブラウザを自動起動するため `ツール起動.bat` を作成。
  - Windows環境での文字化け防止規則に従い、Shift-JISエンコーディングで保存。
  - 実行時に `npm run dev -- --open` を呼び出すように構成し、運用保守を容易化。
- **関連コミット**:
  - `feat: add ツール起動.bat`
- **進捗ステータス**: 全工程完了（進捗100%）。

### 2026-08-29: 白画面不具合の修正
- **変更概要**:
  - TypeScriptの `verbatimModuleSyntax` 設定下で、型のみのインポートがESBuildにより誤ってランタイムコードに残存しブラウザ上で実行エラーとなる問題を解消。
  - `import type { Ingredient }` への修正および不要インポートの削除を実施。
- **関連コミット**:
  - `cd518c9` (`fix: resolve blank screen issue caused by type imports`)

### 2026-08-29: ダークモード対応およびテーマ切り替え機能の追加
- **変更概要**:
  - `ThemeProvider` および `ModeToggle`（太陽/月切り替えボタン）を実装。
  - Tailwind CSS の `darkMode: ["class"]` を有効化し、`App.tsx` 内の配色をダークモード対応に調整。
  - 選択テーマ状態のローカルストレージ永続化に対応。
- **関連コミット**:
  - `b1eb118` (`feat: add dark mode toggle and styling`)

### 2026-08-29: 品質監査の実施および堅牢性の向上
- **変更概要**:
  - `project-quality-audit` スキル基準に基づく全コード深層監査を実施。
  - 検出された問題（QA-001〜QA-004）を即座に修正：
    1. `theme-provider.tsx`: `localStorage` 操作の `try-catch` 保護（プライベートブラウズ環境等でのクラッシュ防止）。
    2. `parser.ts`: `crypto` オブジェクト存在確認の型安全化。
    3. `App.tsx`: 人数の空欄・不正入力時のフォールバック処理強化。
- **関連コミット**:
  - `97f9e5e` (`fix: address audit findings QA-001 to QA-004`)

### 2026-08-29: 品質監査対応・クリーンアップおよび追加機能の実装
- **変更概要**:
  - 未使用の初期ファイル（`src/App.css`）を削除。
  - `parser.test.ts` および `formatter.test.ts` にエッジケース（大文字単位、助数詞と数値の密着整形、端数丸め）のテストケースを追加。
  - `formatter.ts` において助数詞（個、本、枚等）と数値の間に余計なスペースが入る問題を解消。
  - `App.tsx` に以下の3つの便利機能を追加：
    1. **クリップボード一括コピー機能**: 計算後の材料一覧をワンクリックで改行区切りテキストとしてコピー（完了アニメーション・フィードバック付き）。
    2. **一括リセット（クリア）機能**: 入力テキストを消去し、人数をデフォルト値に即座に復元。
    3. **お気に入り / プリセット保存機能**: よく使うレシピを `localStorage` に保存・ワンクリック展開・個別削除できる機能。
  - AI開発コンテキスト管理MCPおよびV3 MCPによる再監査で全9ステップ PASS（スコア 100/100）を確認。
- **関連コミット**:
  - `983e763` (`fix(formatter): 不要ファイル削除、フォーマット修正およびテスト拡充`)
  - `1ebb684` (`feat(ui): クリップボード一括コピー・リセット・お気に入り保存機能の実装`)

### 2026-08-29: AI開発共通CLI監査およびライセンスファイルの配置
- **変更概要**:
  - `AI開発共通CLI`（`ai-dev`）による全方位監査を実施（`doctor`, `context`, `skill`, `sync`）。
  - `ai-dev doctor` で検出された警告に対応し、プロジェクト直下に `LICENSE`（MIT License）を新規作成・配置。
  - `README.md` を正式版に刷新し、機能一覧・技術構成・MIT License 条項全文を明記。
  - 重複していた初期バックアップファイル（`README(20260829-121038).md`）を削除・整理。
  - 再監査にて全20項目 PASS（エラー・警告ゼロ）を確認。
- **関連コミット**:
  - `8986daa` (`docs: LICENSEファイルの新規配置およびREADMEのライセンス条項明記`)

### 2026-08-29: 深層コードレビュー指摘事項（CR-001〜CR-004）の全件修正
- **変更概要**:
  - `parser.ts`: 全角数字・全角スペースの正規化処理および分数表記（`1/2`, `3/4` 等）の正確な数値変換ロジックを実装（CR-001, CR-003）。
  - `formatter.ts`: 丸め後の小さじ端数が 3 以上になった場合の繰り上がり処理（`小さじ3` → `大さじ1`）を追加（CR-002）。
  - `parser.test.ts`, `formatter.test.ts`: 分数、全角、繰り上がりのテストケースを追加（全15テスト合格）。
  - `favorites-list.tsx`: お気に入り一覧コンポーネントを単一責任分離し、`App.tsx` を 270行 にスリム化（CR-004）。
  - V3 MCP による再監査で全9ステップ PASS（スコア 100/100）を確認。
- **関連コミット**:
  - `f662bb7` (`fix(logic): 分数パース・全角正規化・計量単位繰り上がり整数の対応`)
  - `c71fbd3` (`refactor(ui): FavoritesList コンポーネントの単一責任分離と App.tsx のスリム化`)

### 2026-08-29: note・X用デモGIFアニメーションの生成と配置
- **変更概要**:
  - PlaywrightおよびPillowを活用し、CookScaleの一連の操作（レシピ入力 → 人数変更 → 瞬時計算 → コピー → テーマ切り替え）を自動キャプチャ。
  - note記事およびX（旧Twitter）投稿に最適化された軽量デモGIF（`cookscale_demo.gif`: 271.7 KB, 800x700px）を生成。
  - プロジェクト直下、`public/`、および `C:\Users\tk030\Desktop\各種情報\Projects\料理分量らくらく計算ツール\` に配置。
  - `README.md` にデモGIFアニメーションを埋め込み。
- **関連コミット**:
  - `9831889` (`docs: デモGIFアニメーションの追加およびREADMEの更新`)

### 2026-08-29: GitHub Pages 公開準備（ベースパス設定およびデプロイ定義待機）
- **変更概要**:
  - `vite.config.ts` に `base: './'` を追加し、サブディレクトリ配下でのアセット相対パス解決を準備。
  - `docs/github-actions-deploy.yml` に公開用デプロイ定義ファイル（手動トリガー仕様）を待機配置。
  - ビルドが正常に通ることを検証。
- **関連コミット**:
  - `ecc5440` (`chore(deploy): GitHub Pages 公開準備（ベースパス設定およびデプロイ設定の待機配置）`)

### 2026-08-29: リンター警告（未使用変数）の解消
- **変更概要**:
  - `theme-provider.tsx`: `catch (error)` 内の未使用パラメータ `error` を除去し、`catch`（Optional Catch Binding）に統一。
  - リンター（Oxlint）のエラー/警告がゼロ（0 errors）となったことを確認。
- **関連コミット**:
  - （本コミット）

---

### 2026-08-30: 複数材料パース対応・UIコンパクト化および入力ガイドの追加
- **変更概要**:
  - `parser.ts`: `splitRecipeText` 関数を新設。`鶏肉300g 醤油大さじ2` のように同一行内にスペース・カンマ・読点等で複数の材料が並んで入力された場合でも、個別の材料・分量として正確に分割・抽出し、それぞれ指定人数倍率で計算されるように改修。
  - `parser.test.ts`: 単一行複数材料パース、句読点区切り、複数行混在パースのテストケースを追加（全18テスト合格）。
  - `App.tsx`: 
    1. ヘッダータイトル（`CardTitle`）のサイズ縮小と余白調整を行い、画面上部の専有面積をスリム化。
    2. 入力欄ラベルを「レシピ（材料と分量）を貼り付け」から「レシピ（材料と分量）を入力」へ更新。
    3. テキストエリア直下に書き方例と説明（`💡 書き方例: 鶏肉 300g / 醤油 大さじ2 / 卵 2個 / 塩 少々（1行ずつ、またはスペース・読点区切りで入力）`）を配置。
  - `package.json`: `"test": "vitest run"` スクリプトを追加。
  - Playwrightブラウザ検証により、`鶏肉300g 醤油大さじ2`（2人前→3人前）が `鶏肉 450g` / `醤油 大さじ3` に正しく計算・表示されることを確認。
- **関連コミット**:
  - （本コミット）

### 2026-08-30: 旧デモGIFの削除およびREADME整理
- **変更概要**:
  - UI刷新に伴い、旧UI状態のデモGIFファイル（`cookscale_demo.gif`, `public/cookscale_demo.gif`）を削除。
  - `README.md` 内のデモGIF埋め込み記述を削除・整理。
- **関連コミット**:
  - （本コミット）

### 2026-08-30: 最新UI版デモGIFの再作成・配置
- **変更概要**:
  - 改修後の最新UI（コンパクトヘッダー、更新ラベル、書き方ヒント、複数材料計算）に基づき、Playwright自動撮影スクリプトによりデモGIF（`cookscale_demo.gif`: 約241KB）を再作成。
  - プロジェクト直下および `public/` に配置し、`README.md` に埋め込みを再設定。
- **関連コミット**:
  - （本コミット）

### 2026-08-30: リポジトリのパブリック化およびGitHub Pagesデプロイ
- **変更概要**:
  - GitHub CLI によりリポジトリの可視性を `private` から `public` に変更。
  - `.github/workflows/deploy.yml` に GitHub Pages 自動デプロイワークフローを配置。
  - GitHub Pages サイト（Actions 経由）を有効化し、`main` ブランチへのプッシュで自動デプロイを開始。
  - 公開URL: `https://tk030-lotto.github.io/recipe-quantity-calculator/`
  - `README.md` に公開URLリンクを追記。
- **関連コミット**:
  - （本コミット）

### 2026-08-30: リポジトリAbout欄（説明・Webサイト・トピックス）の設定
- **変更概要**:
  - GitHub CLI によりリポジトリの About 欄（Description, Homepage URL, Topics）を設定。
  - Description: `🍳 レシピの人数・分量を一括再計算し、計量しやすい形式（大さじ・小さじ等）に自動変換するブラウザ完結Webツール`
  - Homepage: `https://tk030-lotto.github.io/recipe-quantity-calculator/`
  - Topics: `recipe`, `calculator`, `cooking`, `react`, `typescript`, `vite`, `tailwindcss`, `shadcn-ui`, `spa`
- **関連コミット**:
  - （本コミット）

---

<!-- 運用保守フェーズのエントリはここ以降に追記 -->








