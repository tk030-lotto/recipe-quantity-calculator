# CookScale 仕様書

## 1. 目的

CookScaleは、料理中に発生するレシピの分量変更・単位変換・計量上の面倒を、ブラウザ上で簡単に処理するためのWebツールとする。

本ツールは、汎用的なレシピ管理サービスではなく、**分量計算と計量しやすい表示に特化した単機能ツール**として設計する。

## 2. スコープ

### 2.1 対象機能

- レシピ材料の複数行入力
- 元の人数と作る人数の指定
- 人数変更に伴う分量倍率計算
- g / ml / 大さじ / 小さじ / カップの認識
- 大さじ・小さじの計量しやすい表現への整形
- 「少々」「適量」など数値化できない表現の保持
- スマートフォンでの利用を想定したUI

### 2.2 対象外

- ユーザーアカウント
- ログイン
- レシピのクラウド保存
- SNS機能
- 外部サービス連携
- AI APIへのレシピ送信
- 自動でのレシピ内容生成
- 判断が必要な食材密度換算を無条件に数値化すること

## 3. 入力仕様

### 3.1 材料入力

材料は複数行のテキストとして入力できる。

基本形式：

```text
材料名 数値 単位
```

例：

```text
鶏もも肉 300g
醤油 大さじ2
みりん 大さじ1
砂糖 小さじ2
塩 少々
```

### 3.2 人数入力

- 元レシピ人数：正の数値
- 作りたい人数：正の数値

人数変更時の倍率は、以下で算出する。

```text
倍率 = 作りたい人数 ÷ 元レシピ人数
```

## 4. 分量計算仕様

### 4.1 数値単位

数値として扱える分量は倍率を適用する。

```text
変換後分量 = 元分量 × 倍率
```

### 4.2 g / ml

gおよびmlは、数値をそのまま倍率計算する。

例：

```text
300g × 1.5 = 450g
200ml × 1.5 = 300ml
```

### 4.3 大さじ / 小さじ

大さじ・小さじは倍率計算後、可能な範囲で料理中に扱いやすい表示へ整形する。

例：

```text
大さじ2 × 1.5 = 大さじ3
```

小数が発生した場合は、単純な小数表示だけにせず、大さじ・小さじの組み合わせでの表示を優先する。

### 4.4 カップ

カップは倍率計算を行う対象とする。

国や規格によって容量が異なるため、他単位への換算を行う場合は対象規格を明示できる設計とする。

V1では、単純な倍率変更を優先し、曖昧な容量換算を無理に行わない。

## 5. 数値化しない表現

以下のような表現は、無理に数値化しない。

- 少々
- 適量
- お好みで
- 適宜
- ひとつまみ

基本方針：

**変換できないものは、元の表現を保持する。**

## 6. 食材名の扱い

V1では、食材名を完全に意味解析することを要求しない。

入力された材料名と分量を可能な範囲で分離し、分量を計算する。

食材名の複雑な表記や自由記述については、誤変換を避けることを優先する。

## 7. レシピ貼り付け

複数の材料行を一括貼り付けできることを必須とする。

1行ずつフォームへ入力させる設計を基本としない。

目的は、既存レシピをそのまま貼り付けて短時間で計算できることである。

## 8. 結果表示仕様

結果は、材料ごとに以下を確認できる形式とする。

- 材料名
- 元の分量
- 変換後の分量
- 必要に応じた計量しやすい表現

例：

```text
鶏もも肉    300g       → 450g
醤油        大さじ2    → 大さじ3
みりん      大さじ1    → 大さじ1＋小さじ1.5
砂糖        小さじ2    → 小さじ3
塩          少々       → 少々
```

## 9. UI仕様

### 9.1 基本方針

料理中に使うことを想定し、以下を優先する。

- 大きく見やすい文字
- 少ない操作
- 画面遷移を減らす
- 結果をすぐ確認できる
- スマートフォンでも操作しやすい

### 9.2 入力画面

最低限、以下を配置する。

- レシピ貼り付け欄
- 元の人数
- 作る人数
- 計算実行操作

### 9.3 結果画面

材料ごとの変換結果を一覧表示する。

## 10. 非機能要件

### 10.1 実行環境

- 最新の主要Webブラウザで動作することを目標とする。
- スマートフォンおよびPCの両方で利用できること。

### 10.2 通信

基本的な計算処理はブラウザ内で完結する。

外部APIへの依存を必須としない。

### 10.3 プライバシー

入力されたレシピ内容を外部サーバーへ送信することを前提としない。

### 10.4 エラー耐性

認識できない入力があっても、可能な範囲で他の行の処理を継続する。

変換できない値は、元の表現を保持する。

## 11. 差別化方針

CookScaleは、対応機能の数で競合サービスと競うのではなく、**料理中の使いやすさ**を中心に差別化する。

特に以下を重視する。

1. レシピをそのまま貼り付けられる
2. 人数変更をすぐ反映できる
3. 小数をそのまま見せず、計量しやすい形へ整える
4. 「少々」「適量」を無理に数値化しない
5. スマートフォンで素早く使える
6. 登録・ログイン・課金なしで利用できる

## 12. セキュリティ方針

- 外部通信を必須としない
- 不要なユーザーデータを収集しない
- APIキー等を利用者側へ要求しない
- ブラウザ上で処理できるものはローカル処理を優先する

## 13. 開発方針

最初から多機能化せず、分量変換という中心機能を優先する。

実装では、計算結果の正しさと料理中の操作性を優先する。

将来拡張については、実際の利用状況やユーザーフィードバックを踏まえて必要に応じて検討する。

## 14. ライセンス

MIT License
